<!DOCTYPE html>
<html lang="ar">
<head>
    <meta charset="UTF-8">
    <title>دفعة: {{ batch.name }} - SpeechWise</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
</head>
<body>
    <h1>📁 الملفات في دفعة: {{ batch.name }}</h1>
    <ul>
        {% for audio in batch.audio_files %}
        <li>
            🎧 {{ audio.filename }} — الحالة: {{ audio.status }}
            <a href="{{ url_for('edit_file', audio_id=audio.id) }}">✏️ مراجعة</a>
            | 
            <a href="{{ url_for('download', filename=audio.filename.rsplit('.', 1)[0] + '.txt') }}">⬇️ تحميل النص</a>
        </li>
        {% else %}
        <li>لا توجد ملفات بعد في هذه الدفعة.</li>
        {% endfor %}
    </ul>
    <br>
    <a href="{{ url_for('dashboard') }}">⬅️ الرجوع للوحة التحكم</a>
</body>
</html>
